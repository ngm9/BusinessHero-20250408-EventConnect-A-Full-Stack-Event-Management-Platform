from pydantic import BaseModel, Field
from typing import Optional, List
from datetime import datetime

# TODO: Define Pydantic models for Event creation, update, and response
# Include proper validation for fields
# Consider creating separate models for different operations (create, update, response)